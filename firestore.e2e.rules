rules_version = '2';

// Permissive rules for E2E testing with emulators.
// NOT for production use.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
